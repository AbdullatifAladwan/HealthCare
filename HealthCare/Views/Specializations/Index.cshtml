@model IEnumerable<HealthCare.Models.Specialization>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Specialization1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Specialization1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SpecializationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SpecializationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SpecializationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
